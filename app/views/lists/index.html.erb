<h1>投稿一覧</h1>

<% @lists.each do |list| %>
  <p>タイトル</p>
  <span>
    <%# おすすめの書き方 %>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  
  <p>本文</p>
  <span><%= list.body %></span>
  <hr>
<% end %>